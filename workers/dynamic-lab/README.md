# Dynamic Lab Worker

Planned disposable Android-emulator worker. Active tests require a signed assessment scope and are restricted to explicitly authorized targets. Unknown applications are never executed on the coordinator host.
